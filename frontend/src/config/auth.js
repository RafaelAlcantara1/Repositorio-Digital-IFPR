// Configuração de autenticação
const AUTH_CONFIG = {
  // Tempo de expiração da sessão em milissegundos (24 horas)
  sessionTimeout: 24 * 60 * 60 * 1000,
  // URL base da API
  apiUrl: 'https://repositorio-digital-ifpr.onrender.com'
};

export default AUTH_CONFIG; 